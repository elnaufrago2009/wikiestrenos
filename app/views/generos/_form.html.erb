<%= form_for(@genero, :html => { :class => "form-horizontal" } ) do |f| %>
  <% if @genero.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@genero.errors.count, "error") %> prohibited this genero from being saved:</h2>

      <ul>
      <% @genero.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <div class="control-label">
      <%= f.label :nombre %>
    </div>
    <div class="controls">
      <%= f.text_field :nombre %>
    </div>
  </div>

  <div class="control-group">
    <div class="control-label">
      <%= f.label :resumen %>
    </div>
    <div class="controls">
      <%= f.text_area :resumen, :rows => "4", :class => "span3" %>
    </div>
  </div>

  <div class="control-group">
    <div class="controls">
      <%= f.submit :class => "btn" %>
    </div>
  </div>

<% end %>
